<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Confirmation - Restaurant Order</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <!-- Header -->
  <header class="bg-white shadow-sm border-b">
    <div class="max-w-6xl mx-auto px-4 py-4">
      <div class="flex justify-between items-center">
        <h1 class="text-2xl font-bold text-gray-800">Order Confirmation</h1>
        <a href="/menu" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
          Order More
        </a>
      </div>
    </div>
  </header>

  <!-- Confirmation Content -->
  <main class="max-w-4xl mx-auto px-4 py-8">
    <div class="bg-white rounded-xl shadow-md overflow-hidden">
      <!-- Success Header -->
      <div class="bg-green-600 text-white p-6 text-center">
        <div class="text-6xl mb-4">✅</div>
        <h2 class="text-2xl font-bold mb-2">Order Placed Successfully!</h2>
        <p class="text-green-100">Thank you for your order. We're preparing it for you now.</p>
      </div>
      
      <!-- Order Details -->
      <div class="p-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
          <div class="bg-gray-50 p-4 rounded-lg">
            <h3 class="font-bold text-gray-800 mb-2">Order Information</h3>
            <div class="space-y-1 text-sm">
              <p><strong>Order ID:</strong> <%= order._id.toString().slice(-8).toUpperCase() %></p>
              <p><strong>Table Number:</strong> <%= order.tableNumber %></p>
              <p><strong>Status:</strong> 
                <span class="inline-block px-2 py-1 bg-yellow-100 text-yellow-800 rounded-full text-xs font-medium">
                  <%= order.status.charAt(0).toUpperCase() + order.status.slice(1) %>
                </span>
              </p>
              <p><strong>Order Time:</strong> <%= new Date(order.createdAt).toLocaleString() %></p>
            </div>
          </div>
          
          <div class="bg-gray-50 p-4 rounded-lg">
            <h3 class="font-bold text-gray-800 mb-2">Estimated Time</h3>
            <div class="space-y-1 text-sm">
              <p><strong>Preparation Time:</strong> 15-30 minutes</p>
              <p><strong>Delivery:</strong> To Table <%= order.tableNumber %></p>
              <p class="text-green-600 font-medium">We'll bring your order to your table when ready!</p>
            </div>
          </div>
        </div>
        
        <!-- Order Items -->
        <div class="mb-6">
          <h3 class="text-xl font-bold mb-4">Your Order</h3>
          <div class="space-y-3">
            <% order.items.forEach(item => { %>
              <div class="flex items-center gap-4 p-3 border rounded-lg">
                <img src="<%= item.menuItem.photo %>" alt="<%= item.menuItem.name %>" 
                     class="w-16 h-16 object-cover rounded-lg">
                
                <div class="flex-1">
                  <h4 class="font-semibold"><%= item.menuItem.name %></h4>
                  <p class="text-sm text-gray-600">Quantity: <%= item.quantity %></p>
                  <p class="text-blue-600 font-medium">
                    <%= item.menuItem.price.toLocaleString() %> RWF each
                  </p>
                </div>
                
                <div class="text-right">
                  <p class="font-semibold">
                    <%= (item.menuItem.price * item.quantity).toLocaleString() %> RWF
                  </p>
                </div>
              </div>
            <% }) %>
          </div>
        </div>
        
        <!-- Total -->
        <div class="border-t pt-4">
          <div class="flex justify-between items-center text-2xl font-bold">
            <span>Total Amount:</span>
            <span class="text-blue-600"><%= order.total.toLocaleString() %> RWF</span>
          </div>
          <p class="text-sm text-gray-500 mt-1">Payment due upon delivery</p>
        </div>
        
        <!-- Next Steps -->
        <div class="mt-8 p-4 bg-blue-50 rounded-lg">
          <h3 class="font-bold text-blue-800 mb-2">What happens next?</h3>
          <ul class="text-sm text-blue-700 space-y-1">
            <li>✓ Your order has been sent to our kitchen</li>
            <li>⏳ We're preparing your delicious meal</li>
            <li>🚶 Our staff will deliver it to Table <%= order.tableNumber %></li>
            <li>💳 Payment will be collected upon delivery</li>
          </ul>
        </div>
        
        <!-- Actions -->
        <div class="mt-6 flex gap-4 justify-center">
          <a href="/menu" 
             class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors">
            Order More Items
          </a>
        </div>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white py-8 mt-12">
    <div class="max-w-6xl mx-auto px-4 text-center">
      <p>&copy; 2025 Restaurant Order System</p>
      <p class="text-sm text-gray-400 mt-1">Thank you for dining with us!</p>
    </div>
  </footer>
</body>
</html>